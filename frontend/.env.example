# Backend API base URL without trailing slash (example: http://localhost:8000)
VITE_API_URL=

# Optional fallback sender email used for mailto deep links when no sender is configured
VITE_DEFAULT_SENDER_EMAIL=
